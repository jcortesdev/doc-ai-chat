{
  "name": "@doc-ai-chat/prompts",
  "private": true,
  "version": "0.0.0",
  "description": "Versioned prompts. Every prompt lives in its own file with a tagged version, not as inline strings.",
  "type": "module",
  "exports": {
    "./rag-answer": "./src/rag-answer.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
